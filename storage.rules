rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Location images are readable by everyone
    match /locations/{imageFile} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
